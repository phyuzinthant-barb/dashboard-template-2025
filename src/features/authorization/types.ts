export interface AdminDetail {
  profilePictureUrl: string;
  name: string;
  phone: string;
  email: string;
  role: string;
}

export interface RoleDetail {
  profilePictureUrl: string;
  name: string;
  phone: string;
  email: string;
  role: string;
}
